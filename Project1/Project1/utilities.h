#pragma once
#include <iostream>
#include <string>
#include <Windows.h>
#include <vector>
#include "main.h"
using namespace std;

int initGame(string path);
vector<string> get_all_files_names_within_folder(string folder);